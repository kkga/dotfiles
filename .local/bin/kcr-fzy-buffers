#!/bin/sh

# Open buffers.
#
# Usage:
#
# kcr fzy buffers [patterns]

# – fzy (https://github.com/junegunn/fzf)
# – bat (https://github.com/sharkdp/bat)

kcr get --raw --value buflist |
grep -F "$*" |
fzy -l 24 |

# Open buffers
while read name; do
  kcr send buffer "$name"
done
