#!/bin/sh

menu=$(printf "firefox\nqutebrowser\ncopy to clipboard\nplay" | $DMENU_PROGRAM)

case $menu in
	"firefox")
		firefox "$@" &
		;;
	"qutebrowser")
		qutebrowser "$@" &
		;;
	"copy to clipboard")
		echo "$@" | wl-copy
		;;
	"play")
		mpv "$@" &
		;;
esac
